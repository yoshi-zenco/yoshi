apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases: [../../base]
namespace: cleus-staging
patches:
  - target: { kind: Deployment, name: cleus-api }
    patch: |
      - op: replace
        path: /spec/replicas
        value: 1
